local bridge <const> = require 'lib.bridge.bridge';

---[[
--- EXAMPLE USAGE (CLIENT)
--- bridge:RegisterNetEvent('custom', 'custom:playerLoaded', function(playerData)
---   console.log(("Custom player loaded: %s"):format(json.encode(playerData)));
--- end);
---]]
